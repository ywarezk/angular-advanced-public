import { WithUserDirective } from './with-user.directive';

describe('WithUserDirective', () => {
  it('should create an instance', () => {
    const directive = new WithUserDirective();
    expect(directive).toBeTruthy();
  });
});
